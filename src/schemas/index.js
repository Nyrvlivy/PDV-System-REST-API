const {
    loginUserSchema,
    createUserSchema,
    updateUserSchema,
} = require("./userSchema");
const { createProductSchema } = require("./productSchema");

module.exports = {
    loginUserSchema,
    createUserSchema,
    updateUserSchema,
    createProductSchema,
};
